# Registry Placeholder

This folder is reserved for the future source-copy registry.

Once each bottom navigation variant is stable in the playground, its cleaned source files can be copied here so a CLI can install them into user projects.

